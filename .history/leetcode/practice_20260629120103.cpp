//tcs

#include<bits/stdc++.h>
using namespace std;

int main(){
    int N , V ,C;
    cin>>N>>V>>C;
    int ans = INT_MAX;
    int car = ceil((double)N/(double)4);
    int van = ceil((double)N/)
    for(int vans = 1 ; vans+99<N;vans++){
        int rem = N % (vans)*100;
        int a = (vans*V + )
    }
}